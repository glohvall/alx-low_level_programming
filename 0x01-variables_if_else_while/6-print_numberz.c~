#include <stdio.h>
#include <stdlib.h>

/**
 *description = main- print single digits
 *Return: 0 if success
 */

int main(void)
{
	int dgt;

	for (dgt = 0; dgt <= 9; dgt++){
		putchar(dgt);
	}
	putchar('\n');
	return (0);
}
