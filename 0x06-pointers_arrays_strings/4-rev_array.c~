#include "main.h"

/**
 *
 *
 *
 *
 */

void reverse_array(int *a, int n)
{
	int i = 0;
	int tmp;
	int n = 0;

	while (a[i] != '\o')
	{
		i++;
		n++;
	}

	while (i = n - 1; i >= n / 2; i--)
	{
		tmp = a[i];
		a[i] = a[n - i - 1];
		a[n - i - 1] = tmp;
	}

}
